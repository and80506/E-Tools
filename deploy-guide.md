# 局域网部署与一键运行指南 (Windows 专属)

本工具箱已升级为 **前后端一体化** 的全栈架构，采用 **SQLite 轻量级数据库** 进行集中式数据存储。
只需将本文件夹放置在局域网内任意一台 Windows 电脑上运行，所有局域网设备（包括您的手机、平板、办公电脑）都可以通过浏览器直接访问并共享相同的数据。

---

## 🚀 快速启动 (开箱即用)

1. 双击运行 `start.bat`。
2. 脚本会自动检查环境：
   - 如果是第一次运行，它会自动下载依赖包 (`node_modules`)，并自动编译静态资源 (`dist`)。
   - 自动启动本地服务器引擎。
3. 启动成功后，黑色命令行窗口会显示类似如下的访问地址：
   ```
   =============================================================
      金融投资工具箱 (Value-Invest-Sys) 局域网服务已启动
   =============================================================
    > 本机访问地址:    http://localhost:3000
    > 局域网 IP 访问:
      - http://192.168.1.100:3000  (WLAN)
   ```
4. **本机访问**：直接在电脑浏览器打开 `http://localhost:3000`。
5. **手机/其他设备访问**：让手机连接与电脑相同的 WiFi，在手机浏览器中输入类似 `http://192.168.1.100:3000` 即可实时管理您的自选股，数据将自动同步保存在该 Windows 电脑上。

---

## 🛡️ 局域网访问不通？(Windows 防火墙设置)

如果您可以在本机通过 `localhost:3000` 访问，但手机或其他设备输入 IP 后显示“无法访问此网站”或加载超时，说明被 **Windows 防火墙** 拦截了 3000 端口。

### 解决方法（一键命令，推荐）
1. 点击键盘 `Win` 键，搜索 `cmd`。
2. 右键点击“命令提示符” -> 选择 **“以管理员身份运行”**。
3. 复制并粘贴以下命令后按回车：
   ```cmd
   netsh advfirewall firewall add rule name="Allow Value-Invest-Sys Port 3000" dir=in action=allow protocol=TCP localport=3000
   ```
4. 看到“确定”后，防火墙即已放行。手机重新刷新即可访问。

### 解决方法（手动操作）
1. 按 `Win + R`，输入 `control` 打开控制面板。
2. 进入 `系统和安全` -> `Windows Defender 防火墙` -> 左侧点击 `高级设置`。
3. 点击左侧的 `入站规则`，然后点击右侧的 `新建规则...`。
4. 选择 `端口` -> 下一步 -> `TCP`，特定本地端口输入 `3000` -> 下一步。
5. 选择 `允许连接` -> 勾选所有网络类型 -> 名称填 `Value-Invest-Sys 3000` -> 完成。

---

## ⚙️ 如何设置后台常驻或开机自启 (进阶)

如果您希望这台 Windows 电脑一开机就在后台默默运行 Value-Invest-Sys 服务，而不需要一直开着黑色的 `start.bat` 窗口，可以使用 **PM2**：

1. 在项目文件夹地址栏输入 `cmd`，按回车打开命令行。
2. 安装全局 PM2：
   ```cmd
   npm install pm2 -g
   ```
3. 使用 PM2 启动服务：
   ```cmd
   pm2 start server/index.js --name "Value-Invest-Sys"
   ```
4. （可选）设置开机自启：
   安装 `pm2-windows-startup` 工具：
   ```cmd
   npm install pm2-windows-startup -g
   pm2-startup install
   pm2 save
   ```
现在，即使关闭了命令行窗口，服务依然会在后台稳定运行！

---

## 📂 数据备份与迁移

- **数据位置**：所有的自选股和设置数据均保存在 `data/etools.db`（SQLite数据库文件）中。（如果在某些极端无原生编译环境的 Windows 上，会自动降级保存在 `data/stocks.json` 中）。
- **如何备份**：只需定期复制 `data/` 文件夹即可。
- **如何迁移**：将整个 `Value-Invest-Sys` 文件夹拷贝到新电脑，双击 `start.bat` 即可，您的数据会完好无损地跟着迁移。
